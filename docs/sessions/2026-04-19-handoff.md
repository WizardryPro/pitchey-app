# Session Handoff — 2026-04-19

One-line context: **PR #16 (zod v4) is ready to merge; three follow-up issues are filed and deferred.** Morning priority is merge + cleanup, nothing else.

## Morning commands (copy-paste order)

```bash
cd ~/pitcheymovie/pitchey_v0.2
git status && git log --oneline -5

# Sanity check PR state unchanged overnight
gh pr view 16

# Mark ready if still draft, then merge
gh pr ready 16
gh pr merge 16 --squash --delete-branch --admin

# Local cleanup
git checkout main && git pull
git branch -d zod-v4-migration-2026-04
```

Stop there. Take the morning off this codebase. Anything in this doc beyond the commands above is afternoon/next-week work.

## State of play

### PR #16 — READY TO MERGE
- URL: https://github.com/WizardryPro/pitchey-app/pull/16
- Title: `chore(deps): zod v3 → v4 + better-auth-cloudflare 0.3.0 (closes #15)`
- State: `OPEN`, `isDraft: true`, `mergeable: MERGEABLE`
- Branch: `zod-v4-migration-2026-04` (current branch)
- Validation: curl sign-in/session/sign-out against preview Worker all pass; zod v4 request validation exercised via `ValidationSchemas.userLogin.parse`. Backend tsc = 9 errors (pre-existing baseline, zero introduced). Frontend tsc = 0.
- Honest framing: the Better Auth 1.4.9 → 1.6.5 transitive bump is effectively a type-definition update because the BA adapter isn't actually running in production (see #19). The zod v4 migration is real and tested.

### Issue #18 — preview infrastructure gaps (OPEN, deferred)
- URL: https://github.com/WizardryPro/pitchey-app/issues/18
- Preview environment is Worker-reachable via curl but browser-unreachable. Four layered gaps:
  1. Pages deploys land on retired `pitchey-5o8` project despite workflow config
  2. Frontend CSP hardcoded to production Worker URL, blocks preview Worker fetches
  3. Worker CORS regex narrow; most pages.dev alias patterns rejected
  4. Preview Worker requires runtime secrets via `--var` at deploy time (ephemeral-plaintext in dashboard)
- **First concrete action (30 min, afternoon-sized):** Delete orphan `pitchey-5o8` project in CF dashboard; grep `neon-preview.yml` for that project name and fix deploy target to `pitchey.pages.dev`.
- Blocks browser smoke testing for every future PR → prerequisite for #19's decision.

### Issue #19 — Better Auth adapter disabled since 2025-12-18 (OPEN, decision deferred)
- URL: https://github.com/WizardryPro/pitchey-app/issues/19
- `src/worker-integrated.ts:795` — BA adapter commented out since commit `41850ea1` (buried in a 21-file "lint fixes" commit, +529 net lines in worker-integrated.ts alone).
- Live login path uses custom handlers against legacy `users`/`sessions` tables, writes `pitchey-session` UUID cookie. BA's `session`/`user`/`account`/`verification` tables are empty in prod.
- Three options captured in issue body: (1) rip it out, (2) fix and migrate, (3) document and leave.
- Recommended for this week: **Option 3** — update CLAUDE.md files to describe actual auth. Options 1/2 need #18 fixed first (can't safely migrate auth without a working browser preview).

### Issue #20 — process gap, docs-vs-code drift (OPEN, deferred)
- URL: https://github.com/WizardryPro/pitchey-app/issues/20
- Root cause behind #19: no mechanism audits CLAUDE.md claims against runtime reality. Every Claude session in the past 4 months trusted the docs' "Better Auth — session cookies only" claim and reasoned forward from it.
- Proposed MVP: `scripts/docs-vs-code-audit.sh` scheduled monthly, plus PR-template hygiene and a commit-scope bot for large changes to `worker-integrated.ts`.
- Week-or-two effort. Worth doing **before** Option 1 or 2 from #19, because drift-detection needs to be in place to keep future CLAUDE.md honest after any big auth change.

## Preview-infra housekeeping commits on this branch (included in PR #16)
- `f86b003` — fix preview `VITE_API_URL` (account subdomain was missing)
- `d2b6c00` — bypass `AXIOM_TOKEN` gate in preview via `ENVIRONMENT=preview`
- `cca5a13` — pass `BETTER_AUTH_SECRET` + `ENCRYPTION_KEY` + `MFA_SECRET` as `--vars` to preview worker
- `470e212` — pass secrets as `--var` bindings so worker can read them at runtime
- `6c6ad32` — stop leaking preview DB password in auto-posted PR comment

These benefit all future preview deploys. They do not close #18 (the CSP/CORS/orphan-project gaps remain).

## Priority order after merge

1. **#18** — preview infra (30-min first action, unblocks browser smoke-testing)
2. **#19** — do Option 3 (docs update) this week; defer Option 1/2 decision until #18 is done
3. **#20** — process/drift-audit, week-or-two effort, do before committing to a big #19 migration

## What NOT to do tomorrow morning

- Don't start on #18/#19/#20 before merging #16 — dependencies pile up fast.
- Don't re-investigate the BA situation; #19 body captures the full grep sweep and three options.
- Don't trust CLAUDE.md's "Auth: Better Auth — session cookies only, no JWT" claim until #19's Option 3 doc update lands. The live path is custom handlers + legacy `sessions` table + `pitchey-session` cookie.

## Memory to update (claude.ai side, not repo)

Next conversation on claude.ai, say: "Update memory: Better Auth adapter is commented out since 2025-12-18, login uses custom handlers against legacy sessions table. Issue #19 tracks the decision." Otherwise the claude.ai memory and the repo reality will disagree on auth architecture for a while.
