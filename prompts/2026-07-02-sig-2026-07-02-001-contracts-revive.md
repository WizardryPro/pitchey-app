---
id: prompt-2026-07-02-002
date: 2026-07-02
source_signal: sig-2026-07-02-001#contracts-revive
signal_status: needs-human
band: 2
gates: optional
action: investigate
---

> Target stack: Cloudflare Worker — single hand-rolled `RouteRegistry` (NOT Hono, NOT Express) in `src/worker-integrated.ts` · React 18 + Vite + Zustand + Tailwind on Cloudflare Pages · Neon PostgreSQL via `@neondatabase/serverless` + `postgres` driver, **raw SQL only (no ORM, Drizzle fully retired)** · **custom session auth** (`pitchey-session` UUID cookie + JWT bearer fallback — Better Auth was ripped 2026-05-04) · same-origin via Pages Functions proxy (`frontend/functions/api/[[path]].ts`)

# Investigate: is `src/handlers/contracts.ts` safe to delete, or earmarked for REVIVE cluster #308?

**Action:** investigate — the verifier confirmed the file is technically orphaned (zero active imports), but the orphan-catalog override for the REVIVE cluster (#308) applies. The open question must be answered by a human before this file can be included in a batch-deletion prompt.

## Open question

> Is `src/handlers/contracts.ts` intended for revival as part of the contracts-workflow feature tracked in issue #308 — making it a parked reference to keep — or has that revival been deferred/cancelled to the point where the file is safe to delete alongside the 8 confirmed orphans?

## Background (verified — do not re-derive)

The `dead-route-sweep` verifier confirmed that `src/handlers/contracts.ts` has **zero active imports** anywhere in `src/`. The `ContractHandlers` class (12 methods: `createContract`, `listContracts`, `getContract`, `sendForSignature`, `signContract`, `generatePDF`, `terminateContract`, `listTemplates`, `createTemplate`, `amendContract`, `getVersions`, `bulkSendContracts`) is wired to nothing in the live `worker-integrated.ts`.

However, the REVIVE cluster override in the orphan catalog flags this file as belonging to the contracts-workflow feature tracked in **issue #308** — meaning the team may be keeping it intentionally as a reference point for a future revival, analogous to the PROTECTED parked paths (`src/workflows/` #60, `src/workers/crawl4ai-worker.ts` #61).

The distinction matters: if it is REVIVE-earmarked, deleting it is a product decision (needs Karl), not a cleanup task. If it is not, it belongs in the batch-deletion prompt at `prompts/2026-07-02-sig-2026-07-02-001-batch-delete-8-confirmed-orphans.md`.

## Verify first — steps

1. Read `src/handlers/contracts.ts` (do not edit): understand what the 12 handler methods do and what external dependencies they reference (DocuSign? Stripe? a contracts DB table?).
2. Check issue #308 (via `mcp__github__issue_read` or `mcp__github__list_issues`): read its current state, labels, and comments. Is it open/closed? Is there an explicit "keep as reference" or "safe to delete" decision?
3. Grep for any schema migration that defines a `contracts` table:
   ```bash
   grep -rl "contracts" src/db/migrations/ | head -10
   ```
   If no contracts table exists in prod (check `schema_migrations`), the revival would require new schema — raising the cost of reviving vs rebuilding from scratch.
4. Check the `CLAUDE.md` PROTECTED paths section: does `contracts.ts` appear in the parked-path list alongside `src/workflows/` and the crawl4ai files? If yes, it is explicitly protected. If not, the issue #308 reference may be informal.

## Done When

A written verdict in this prompt's body (append it here before closing) stating:

> **Verdict [YYYY-MM-DD]:** [keep-earmarked | safe-to-delete]
>
> Reason: [1–2 sentences — what issue #308 says, whether a contracts table exists, whether explicit keep/delete decision was recorded]
>
> Next action: [if safe-to-delete → add contracts.ts to the batch-delete prompt and delete it; if keep-earmarked → no code change, close this investigation]

**No source edits in this prompt.** Investigation only. Source changes (if any) belong in a follow-up execute prompt.
