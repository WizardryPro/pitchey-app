---
id: prompt-2026-07-02-003
date: 2026-07-02
source_signal: sig-2026-07-02-001#ndas-routes-intent
signal_status: needs-human
band: 2
gates: optional
action: investigate
---

> Target stack: Cloudflare Worker — single hand-rolled `RouteRegistry` (NOT Hono, NOT Express) in `src/worker-integrated.ts` · React 18 + Vite + Zustand + Tailwind on Cloudflare Pages · Neon PostgreSQL via `@neondatabase/serverless` + `postgres` driver, **raw SQL only (no ORM, Drizzle fully retired)** · **custom session auth** (`pitchey-session` UUID cookie + JWT bearer fallback — Better Auth was ripped 2026-05-04) · same-origin via Pages Functions proxy (`frontend/functions/api/[[path]].ts`)

# Investigate: is `src/routes/ndas.ts` a planned refactor-in-progress or permanently superseded?

**Action:** investigate — the verifier confirmed the file is technically orphaned (import commented out), but it is unclear whether this represents an abandoned refactor or active intent. The open question must be answered before this file can be included in a batch-deletion prompt.

## Open question

> Is `src/routes/ndas.ts` a refactor-in-progress (i.e., work was started to extract NDA routing into a dedicated routes file but stalled) — or is it permanently superseded by the inline NDA handling in `worker-integrated.ts`, making it safe to delete?

## Background (verified — do not re-derive)

The `dead-route-sweep` verifier confirmed:

- `src/routes/ndas.ts` import is **commented out** at `worker-integrated.ts:364`:
  ```ts
  // import { ndasRoutes } from './routes/ndas'
  ```
- Live NDA functionality is fully served by **two active paths** in `worker-integrated.ts`:
  - `handlers/nda.ts` (active import at line 50)
  - Inline private methods registered at lines 2433–2457 (`this.requestNDA`, `this.approveNDA`, etc.)
- The routes file uses camelCase method names (`requestNda`) that differ from the live inline methods (`requestNDA`) — suggesting the file predates or diverged from the live implementation.
- No other file in `src/` actively imports `ndasRoutes` or anything from `src/routes/ndas.ts`.

The ambiguity: the commented-out import could mean (a) a refactor was started to consolidate NDA routes but not finished, in which case someone may plan to un-comment and complete it; or (b) the inline approach became canonical and the routes file is just an orphan that was never cleaned up.

## Verify first — steps

1. Read `src/routes/ndas.ts` (do not edit): compare its route list and method signatures against the live inline NDA methods in `worker-integrated.ts` (grep `requestNDA\|approveNDA\|rejectNDA\|getNDAStatus` in the file). Do they cover the same endpoints? Are there routes in `ndas.ts` with no live equivalent?
2. Check git log for `src/routes/ndas.ts`:
   ```bash
   git log --oneline --follow -- src/routes/ndas.ts | head -15
   ```
   When was it last touched? Was it being actively developed or has it been stale for months?
3. Check git log for when the import was commented out in `worker-integrated.ts`:
   ```bash
   git log -S "ndasRoutes" --oneline src/worker-integrated.ts | head -5
   ```
   The commit that added the `//` comment explains the intent — was it a "parking for later" or a "replacing this with inline"?
4. Check whether `src/routes/ndas.ts` exports anything not present in the live inline NDA handlers. If the routes file has endpoint coverage gaps vs the live inline handlers, deleting it loses nothing. If it has endpoints the live path lacks, that's signal the file was a planned addition.

## Done When

A written verdict appended to this prompt's body:

> **Verdict [YYYY-MM-DD]:** [safe-to-delete | keep-pending-refactor]
>
> Reason: [1–2 sentences — last-touched date, whether routes match or diverge, what the comment-out commit message said]
>
> Next action: [if safe-to-delete → add ndas.ts to the batch-delete prompt and delete it; if keep-pending-refactor → record the intended refactor scope here and close investigation without code change]

**No source edits in this prompt.** Investigation only. Source changes (if any) belong in a follow-up execute prompt.
