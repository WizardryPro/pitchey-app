---
id: prompt-2026-06-25-009
date: 2026-06-25
source_signal: sig-2026-06-25-009
signal_status: confirmed
band: 1
gates: recommended
action: execute
---

> Target stack: Cloudflare Worker — hand-rolled `RouteRegistry` in `src/worker-integrated.ts` (no framework) · Neon Postgres via `@neondatabase/serverless` + `postgres` driver, **raw SQL only** · custom `pitchey-session` auth · **the worker is esbuild-bundled and NOT type-checked in CI** (resolved from STACK.md)

## Task: Add missing Env members (UPSTASH/STRIPE) — 5× TS2339 in `src/worker-modules/admin-endpoints.ts`

### Context
`type-lint-audit` confirmed 5× `TS2339` in `src/worker-modules/admin-endpoints.ts` (lines 1769-1778): the `Env` type is missing `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, and `STRIPE_SECRET_KEY` — these exist as real runtime bindings/secrets but aren’t declared on the `Env` interface. This is the live `/api/admin/*` intercept handler (imported `worker-integrated.ts:368`, instantiated :777). Signal `sig-2026-06-25-009`.

### Fix
1. Find the `Env` interface/type definition. Add the three missing members with correct types (`string` for the Upstash URL/token and the Stripe secret).
2. Confirm these bindings/secrets are genuinely provisioned (grep `wrangler.toml` + other `env.UPSTASH_REDIS_REST_URL` / `env.STRIPE_SECRET_KEY` usages) so the type matches reality — they should already be used elsewhere in the worker.

### Constraints
- Scope the change to the file(s) named above. Do **not** silence the checker with `any`, `as` casts, or `@ts-ignore` — fix the actual type mismatch.
- Do not touch PROTECTED parked paths (`src/workflows/`, crawl4ai/console-analysis — #60/#61).

### Done When
- [ ] `./frontend/node_modules/.bin/tsc --noEmit -p tsconfig.json` shows the cited error(s) gone **and introduces no new errors** (diff the before/after error counts — the worker has no CI type-check, so this gate is the only safety net)
- [ ] `npm run build:worker` still bundles clean
